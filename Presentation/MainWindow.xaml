<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Presentation.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Presentation">

    <Grid Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" RowSpacing="12" Padding="32">
        <Grid.RowDefinitions >
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="48"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <!-- Ordenação -->
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>

            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                
                <!-- Ordenação -->
                <ComboBox x:Name="cboTipoDeOrdenacao"
                          DisplayMemberPath="Valor"
                          Padding="8,0,0,0" 
                          Height="32"
                          Width="130"
                          ItemsSource="{Binding TipoDeOrdenacao}"
                          SelectedItem="{Binding TipoDeOrdenacaoSelecionado, Mode=TwoWay}"
                          HorizontalContentAlignment="Center"
                          VerticalAlignment="Center"/>
                <Button Grid.Column="1" Padding="0" Width="32" Height="32" Style="{StaticResource AlternateCloseButtonStyle}">
                    <FontIcon Glyph="&#xE8CB;" FontFamily="Segoe Fluent Icons" FontSize="18"/>
                </Button>
                
            </StackPanel>
                                                            
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                <!-- Pesquisa -->
                <ComboBox x:Name="cboTipoDePesquisa"
                          DisplayMemberPath="Valor"
                          Padding="8,0,0,0" 
                          Height="32"
                          Width="130"
                          ItemsSource="{Binding TipoDePesquisa}"
                          SelectedItem="{Binding TipoDePesquisaSelecionado, Mode=TwoWay}"
                          HorizontalContentAlignment="Center"
                          VerticalAlignment="Center"/>

                <TextBox x:Name="txtPesquisa" Height="26" MinWidth="300"  Padding="8,6,0,0"/>

                <Button x:Name="btnPesquisar" Grid.Column="1" Padding="0,0,0,2" Width="110" VerticalAlignment="Center" Height="32" Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8" Padding="0" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE721;" FontFamily="Segoe Fluent Icons" VerticalAlignment="Center" FontSize="18"/>
                        <TextBlock Text="Pesquisar" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                <Button x:Name="btnConfigurar" Grid.Column="1" Padding="0,0,0,2" Width="32" VerticalAlignment="Center" Height="32" Style="{StaticResource AlternateCloseButtonStyle}">
                    <FontIcon Glyph="&#xE713;" FontFamily="Segoe Fluent Icons" VerticalAlignment="Center" FontSize="18"/>
                </Button>
                <Button x:Name="btnAdicionar" Grid.Column="1" Padding="0" Width="120" VerticalAlignment="Center" Height="32" Style="{StaticResource DefaultButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8" Padding="0" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE710;" FontFamily="Segoe Fluent Icons" VerticalAlignment="Center" FontSize="18"/>
                        <TextBlock Text="Adicionar" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>

</Window>
