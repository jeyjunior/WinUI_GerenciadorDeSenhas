<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="GerenciadorDeSenhas.Views.RegistrarLoginDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GerenciadorDeSenhas.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    CornerRadius="10"
    PrimaryButtonText="Salvar"
    SecondaryButtonText="Cancelar"
    DefaultButton="Primary"
    PrimaryButtonClick="SalvarCredencial_Click"
    SecondaryButtonStyle="{StaticResource DefaultButtonStyle}" Loaded="ContentDialog_Loaded">

    <StackPanel>
        <TextBlock  Text="Registrar Login" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold" Padding="0" Margin="0"/>
        <StackPanel x:Name="spPrincipal" 
                    Width="500"
                    HorizontalAlignment="Stretch" 
                    VerticalAlignment="Stretch" 
                    Spacing="8" 
                    Background="{StaticResource Nenhuma}" 
                    Padding="5" 
                    CornerRadius="10">

            <StackPanel Orientation="Horizontal" Spacing="10" Padding="0">
                <TextBlock Text="Nome" Margin="0" Foreground="{StaticResource Cinza1}"/>
                <FontIcon x:Name="fontIconNome" Glyph="&#xE783;" Foreground="{StaticResource Amarelo}" Visibility="Collapsed" FontFamily="Segoe Fluent Icons" FontSize="18"/>
            </StackPanel>
            <TextBox x:Name="txtNome" TextChanged="txtNome_TextChanged" MinWidth="468" MaxLength="128" BorderBrush="{StaticResource Nenhuma}" BorderThickness="2" HorizontalAlignment="Stretch" />

            <StackPanel Orientation="Horizontal" Spacing="10" Padding="0">
                <TextBlock Text="Usuário" Margin="0,8,0,0" Foreground="{StaticResource Cinza1}"/>
                <FontIcon x:Name="fontIconUsuario" Glyph="&#xE783;" Foreground="{StaticResource Amarelo}" Visibility="Collapsed" FontFamily="Segoe Fluent Icons" FontSize="18"/>
            </StackPanel>
            <TextBox x:Name="txtUsuario" TextChanged="txtUsuario_TextChanged" MinWidth="468" MaxLength="128" BorderBrush="{StaticResource Nenhuma}" BorderThickness="1" HorizontalAlignment="Stretch" />

            <StackPanel Orientation="Horizontal" Spacing="10" Padding="0">
                <TextBlock Text="Senha" Margin="0,8,0,0" Foreground="{StaticResource Cinza1}"/>
                <FontIcon x:Name="fontIconSenha" Glyph="&#xE783;" Foreground="{StaticResource Amarelo}" Visibility="Collapsed" FontFamily="Segoe Fluent Icons" FontSize="18"/>
            </StackPanel>
            <PasswordBox x:Name="passBoxSenha" PasswordChanged="passBoxSenha_PasswordChanged" MinWidth="468" MaxLength="128" BorderBrush="{StaticResource Nenhuma}" BorderThickness="1" HorizontalAlignment="Stretch" />
        </StackPanel>
    </StackPanel>
</ContentDialog>
